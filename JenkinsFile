pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'ls -lrth'
                sh 'npm -version'
                sh 'npm run build'
            }
        }
    }
}
