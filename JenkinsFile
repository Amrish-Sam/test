pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'ls -lrth'
                sh 'npm install -force'
                sh 'npm run build'
            }
        }
    }
}
