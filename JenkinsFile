pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'ls -lrth'
                sh 'npm install'
                sh 'npm run build'
            }
        }
    }
}
